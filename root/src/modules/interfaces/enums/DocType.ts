
export enum DocType {

    None = "none",
    Tree = `tree`,
    Node = `node`,
    Edge = `edge`,
    Subtree = 'subtree',
    StRoot = `stRoot`,
    StRootEdge = `stRootEdge`,
    StRootLink = `stRootLink`,
    StSocket = `stSocket`,
    StSocketEdge = `stSocketEdge`,
    StSocketLink = `stSocketLink`,
    StSocketHoleMapping = `stSocketHoleMapping`,
    NotCodedFor = 'Not coded for in database yet...'
}

